# LearnKit 📚

학습 목표 관리와 뽀모도로 타이머를 결합한 스마트 학습 도우미 앱

## 📋 프로젝트 개요

LearnKit은 학습 목표 설정, 뽀모도로 타이머, 단어장, 학습 통계 기능을 제공하는 크로스 플랫폼 학습 관리 애플리케이션입니다.

### 주요 기능
- 🎯 **학습 목표 관리**: 목표 설정 및 진행률 추적
- ⏱️ **뽀모도로 타이머**: 25분 집중 + 5분 휴식 사이클
- 📖 **단어장**: 플래시카드 기반 단어 학습
- 📊 **학습 통계**: 주간/월간 학습 현황 분석
- 📅 **일정 관리**: 학습 스케줄 등록 및 알림

---

## 🛠️ 기술 스택

| 구분 | 기술 | 버전 |
|------|------|------|
| **Frontend** | Flutter | 3.35.4 |
| **Frontend 언어** | Dart | 3.9.2+ |
| **Backend** | Spring Boot | 3.5.6 |
| **Backend 언어** | Java | 21 |
| **Database** | MySQL | 8.x |
| **Storage** | Supabase | - |

---

## 🚀 실행 방법

### 사전 요구사항

- **Java 21** 이상
- **Flutter 3.35.4** 이상
- **MySQL 8.x**
- **Android Studio** 또는 **VS Code**

### 1. Backend 실행

```bash
# 프로젝트 루트에서 backend 폴더로 이동
cd backEnd/learnkit-backend

# Gradle 빌드 (Mac/Linux)
./gradlew build

# Gradle 빌드 (Windows)
gradlew.bat build

# 애플리케이션 실행
./gradlew bootRun
```

> ⚠️ 실행 전 `src/main/resources/application.properties`에서 데이터베이스 연결 정보를 설정해주세요.

#### application.properties 설정 예시
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/learnkit
spring.datasource.username=your_username
spring.datasource.password=your_password
spring.jpa.hibernate.ddl-auto=update
```

### 2. Frontend 실행

```bash
# 프로젝트 루트에서 front 폴더로 이동
cd front

# 의존성 설치
flutter pub get

# iOS 실행 (Mac에서만 가능)
flutter run -d ios

# Android 실행
flutter run -d android

# 웹 실행
flutter run -d chrome
```

#### API 서버 주소 설정
`lib/config/api_config.dart` 파일에서 백엔드 서버 주소를 설정합니다:
```dart
class ApiConfig {
  static const String baseUrl = 'http://localhost:8080';  // 로컬 개발
  // static const String baseUrl = 'http://your-server-ip:8080';  // 실제 서버
}
```

---

## 📁 프로젝트 구조

```
LearnKit/
├── backEnd/
│   └── learnkit-backend/
│       ├── src/main/java/com/learnkit/backend/
│       │   ├── controller/     # REST API 컨트롤러
│       │   ├── service/        # 비즈니스 로직
│       │   ├── repository/     # 데이터 액세스
│       │   ├── domain/         # 엔티티 클래스
│       │   ├── dto/            # 데이터 전송 객체
│       │   └── exception/      # 예외 처리
│       └── build.gradle
│
├── front/
│   ├── lib/
│   │   ├── screens/           # 화면 위젯
│   │   ├── widgets/           # 공통 위젯
│   │   ├── models/            # 데이터 모델
│   │   ├── services/          # API 서비스
│   │   ├── providers/         # 상태 관리
│   │   ├── config/            # 설정
│   │   └── main.dart          # 앱 진입점
│   └── pubspec.yaml
│
└── README.md
```

---

## 📊 API 명세

### 주요 API 엔드포인트

| 분류 | 메서드 | 엔드포인트 | 설명 |
|------|--------|-----------|------|
| 사용자 | POST | `/api/users` | 회원가입 |
| 사용자 | GET | `/api/users/{id}` | 사용자 조회 |
| 목표 | POST | `/api/goals` | 목표 생성 |
| 목표 | GET | `/api/goals/user/{userId}` | 사용자별 목표 조회 |
| 학습 세션 | POST | `/api/goal-sessions` | 학습 세션 기록 |
| 단어장 | POST | `/api/wordbooks` | 단어장 생성 |
| 카드 | POST | `/api/cards` | 카드 생성 |
| 통계 | GET | `/api/stats/weekly/{userId}` | 주간 통계 조회 |

전체 API 명세는 54개 엔드포인트로 구성되어 있습니다.

---

## 🗄️ 데이터베이스 스키마

### 주요 테이블

| 테이블명 | 설명 |
|----------|------|
| `user` | 사용자 정보 |
| `goal` | 학습 목표 |
| `goal_study_session` | 목표별 학습 세션 |
| `word_book` | 단어장 |
| `card` | 단어 카드 |
| `word_book_study_session` | 단어장 학습 세션 |
| `schedule` | 일정 |
| `weekly_goal_baseline` | 주간 목표 기준선 |
| `weekly_card_baseline` | 주간 카드 기준선 |
| `app_launch` | 앱 실행 기록 |

---

## 📱 화면 구성

| 화면 | 설명 |
|------|------|
| 홈 화면 | 오늘의 학습 현황 및 빠른 접근 |
| 목표 목록 | 등록된 학습 목표 리스트 |
| 목표 상세 | 목표 진행률 및 세션 기록 |
| 뽀모도로 | 집중 타이머 및 휴식 관리 |
| 단어장 목록 | 단어장 리스트 |
| 단어장 상세 | 카드 목록 및 학습 |
| 학습 화면 | 플래시카드 학습 |
| 통계 | 주간/목표별 학습 분석 |
| 일정 관리 | 학습 스케줄 등록 |
| 설정 | 앱 환경 설정 |

---

## 👥 개발자

- **김용진** - Frontend & Backend 개발

---

## 📄 라이선스

이 프로젝트는 학습 목적으로 개발되었습니다.

---

## 📝 버전 히스토리

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0.0 | 2025.12.09 | 초기 릴리즈 |
